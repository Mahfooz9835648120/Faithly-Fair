export type Product = { id:string; name:string; slug:string; description:string; price:number; stock_quantity:number; featured:boolean; active:boolean; display_order:number; category?:'bouquet'|'hamper'; image_url?:string };
export type CartItem = { product:Product; quantity:number };
export type Hero = { eyebrow:string; title:string; description:string; cta_label:string; cta_link:string; image_url?:string };
export type Settings = { business_name:string; email:string; whatsapp:string; upi_id:string; upi_payee_name:string };
export type Order = { id:string; order_number:string; customer_name:string; mobile:string; email?:string; address_line1:string; address_line2?:string; landmark?:string; city:string; state:string; pincode:string; payment_method:'cod'|'upi'; payment_status:string; order_status:string; total:number; upload_token:string; created_at:string; order_items?:Array<{product_name:string;unit_price:number;quantity:number;line_total:number}>; payment_proofs?:Array<{id:string;storage_path:string;created_at:string}> };
